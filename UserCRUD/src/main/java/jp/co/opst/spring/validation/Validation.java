package jp.co.opst.spring.validation;

public class Validation {

	// @Autowired
	// private Properties validationProperties;
	//
	// public Map<String, String> validation(Object userId) {
	// Map<String, String> errorMap = new HashMap<>();
	// System.out.println(validationProperties);
	//
	// if (!String.class.isInstance(userId)) {
	// errorMap.put("userIdError", validationProperties.getProperty("userId") +
	// validationProperties.getProperty("invalid"));
	// return errorMap;
	// }
	//
	// if (LengthUtil.isEmpty((String)userId)) {
	// errorMap.put("userIdError", validationProperties.getProperty("userId") +
	// validationProperties.getProperty("required"));
	// return errorMap;
	// }
	//
	// if (IntegerUtil.isNotInteger(userId)) {
	// errorMap.put("userIdError", validationProperties.getProperty("userId") +
	// validationProperties.getProperty("notinteger"));
	// }
	//
	// return errorMap;
	// }

}
